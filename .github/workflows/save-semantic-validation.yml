name: Save semantic validation checks

on:
  pull_request:
    branches: ["main"]
    paths:
      - "main_v19_release.gd"
      - "tests/save_semantic_validation_test.gd"
      - ".github/workflows/save-semantic-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  save-semantic-validation:
    runs-on: ubuntu-24.04
    container:
      image: barichello/godot-ci:4.7.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Godot version
        run: godot --version

      - name: Semantic save fixture
        run: godot --headless --path . --script res://tests/save_semantic_validation_test.gd
